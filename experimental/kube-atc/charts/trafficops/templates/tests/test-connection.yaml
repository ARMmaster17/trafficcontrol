apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trafficops.fullname" . }}-test-connection"
  labels:
    {{- include "trafficops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trafficops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
