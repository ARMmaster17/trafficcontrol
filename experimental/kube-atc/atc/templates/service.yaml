apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-atc.fullname" . }}
  labels:
    {{- include "kube-atc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-atc.selectorLabels" . | nindent 4 }}
