apiVersion: v1
kind: Service
metadata:
  name: {{ include "ciab-kube.fullname" . }}
  labels:
    {{- include "ciab-kube.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ciab-kube.selectorLabels" . | nindent 4 }}
