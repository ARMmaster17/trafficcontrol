apiVersion: v1
kind: Service
metadata:
  name: {{ include "traffic-router.fullname" . }}
  labels:
    {{- include "traffic-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "traffic-router.selectorLabels" . | nindent 4 }}
