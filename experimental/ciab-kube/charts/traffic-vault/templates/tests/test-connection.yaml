apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "traffic-vault.fullname" . }}-test-connection"
  labels:
    {{- include "traffic-vault.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "traffic-vault.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
