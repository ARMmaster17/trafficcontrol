apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "traffic-stats.fullname" . }}-test-connection"
  labels:
    {{- include "traffic-stats.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "traffic-stats.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
