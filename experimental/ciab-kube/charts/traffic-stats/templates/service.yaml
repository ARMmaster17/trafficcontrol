apiVersion: v1
kind: Service
metadata:
  name: {{ include "traffic-stats.fullname" . }}
  labels:
    {{- include "traffic-stats.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "traffic-stats.selectorLabels" . | nindent 4 }}
