apiVersion: v1
kind: Service
metadata:
  name: {{ include "enroller.fullname" . }}
  labels:
    {{- include "enroller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enroller.selectorLabels" . | nindent 4 }}
