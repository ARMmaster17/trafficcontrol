apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enroller.fullname" . }}-test-connection"
  labels:
    {{- include "enroller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enroller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
