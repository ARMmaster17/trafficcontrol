apiVersion: v1
kind: Service
metadata:
  name: {{ include "mid.fullname" . }}
  labels:
    {{- include "mid.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mid.selectorLabels" . | nindent 4 }}
