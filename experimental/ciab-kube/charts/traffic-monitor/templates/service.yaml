apiVersion: v1
kind: Service
metadata:
  name: {{ include "traffic-monitor.fullname" . }}
  labels:
    {{- include "traffic-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "traffic-monitor.selectorLabels" . | nindent 4 }}
