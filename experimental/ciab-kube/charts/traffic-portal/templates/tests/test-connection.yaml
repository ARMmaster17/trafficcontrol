apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "traffic-portal.fullname" . }}-test-connection"
  labels:
    {{- include "traffic-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "traffic-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
